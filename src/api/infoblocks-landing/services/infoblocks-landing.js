'use strict';

/**
 * infoblocks-landing service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::infoblocks-landing.infoblocks-landing');
