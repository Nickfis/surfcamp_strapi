'use strict';

/**
 * infoblocks-experience service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::infoblocks-experience.infoblocks-experience');
