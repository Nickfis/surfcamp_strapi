'use strict';

/**
 * infoblocks-experience controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::infoblocks-experience.infoblocks-experience');
